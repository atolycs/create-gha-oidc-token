{
  "name": "create-gha-oidc-token",
  "version": "1.0.0",
  "description": "Github Action Create Github Access from Apps with OIDC",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "biome:format": "biome check --write .",
    "build": "$npm_execpath run biome:format && node ./scripts/build.mjs"
  },
  "keywords": [],
  "author": "Atolycs",
  "license": "MIT",
  "devDependencies": {
    "@actions/http-client": "^2.2.3",
    "@atolycs/biome-config": "^0.2.5",
    "@github/local-action": "^6.0.0",
    "@types/node": "^24.5.1",
    "esbuild": "^0.25.9",
    "glob": "^11.0.3",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  }
}
